# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# For local development with SQLite (default)
# No need to change this for local development
DATABASE_URL=sqlite:./database.sqlite

# For production with PostgreSQL (Railway)
# Uncomment and replace with your actual PostgreSQL connection string when deploying
# DATABASE_URL=postgresql://username:password@hostname:port/database

# CORS Configuration
# Uncomment and set to your frontend URL in production
# CORS_ORIGIN=https://your-netlify-app.netlify.app
